import { ProductForm } from "@/components/admin/product-form";import { createProduct } from "@/lib/admin-products";import { prisma } from "@/lib/prisma";
export default async function NewProduct({searchParams}:{searchParams:Promise<{error?:string}>}){const [categories,q]=await Promise.all([prisma.category.findMany({select:{id:true,name:true},orderBy:{name:'asc'}}),searchParams]);return <><h1 className="type-h1 mb-6">Новый товар</h1><p className="mb-4 text-muted">Изображения можно добавить после создания товара на странице редактирования.</p><ProductForm action={createProduct} categories={categories} error={q.error}/></>}
