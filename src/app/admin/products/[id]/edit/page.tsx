import { notFound } from "next/navigation";import { ProductForm } from "@/components/admin/product-form";import { getAdminProductById,updateProduct } from "@/lib/admin-products";import { prisma } from "@/lib/prisma";
import { ProductImageManager } from "@/components/admin/product-image-manager";
export default async function EditProduct({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{error?:string}>}){const{id}=await params;const [product,categories,q]=await Promise.all([getAdminProductById(id),prisma.category.findMany({select:{id:true,name:true},orderBy:{name:'asc'}}),searchParams]);if(!product)notFound();const action=updateProduct.bind(null,id,product.slug);return <><h1 className="type-h1 mb-6">Редактирование товара</h1><ProductForm action={action} categories={categories} initial={product} error={q.error}/><ProductImageManager productId={id} initialImages={product.images}/></>}
