import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input";
export function CategoryForm({action,categories,initial,error}:{action:(d:FormData)=>void|Promise<void>;categories:Array<{id:string;name:string}>;initial?:{name?:string;slug?:string;description?:string|null;parentId?:string|null;order?:number;featured?:boolean};error?:string}){return <form action={action} className="max-w-2xl space-y-4 rounded-lg border border-border bg-surface p-6">{[['name','Название'],['slug','Slug'],['description','Описание'],['order','Порядок']].map(([n,l])=><label key={n} className="block"><span className="mb-1 block text-sm font-semibold">{l}</span><Input name={n} type={n==='order'?'number':'text'} defaultValue={String(initial?.[n as keyof typeof initial]??(n==='order'?0:''))} required={n!=='description'}/></label>)}<label className="block"><span className="mb-1 block text-sm font-semibold">Родитель</span><select name="parentId" defaultValue={initial?.parentId??''} className="h-11 w-full rounded-md border border-border bg-white px-3"><option value="">Нет</option>{categories.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label><label className="flex gap-2"><input name="featured" type="checkbox" defaultChecked={initial?.featured}/>Featured</label>{error&&<p className="text-destructive">{error}</p>}<Button type="submit">Сохранить</Button></form>}
