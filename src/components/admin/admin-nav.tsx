import { Boxes, FolderTree, LayoutDashboard, PackageCheck, Store } from "lucide-react"; import Link from "next/link";
const links=[[/admin$/, "/admin","Обзор",LayoutDashboard],[null,"/admin/products","Товары",Boxes],[null,"/admin/categories","Категории",FolderTree],[null,"/admin/orders","Заказы",PackageCheck],[null,"/","На сайт",Store]] as const;
export function AdminNav({className=""}:{className?:string}){return <nav className={`flex flex-wrap gap-2 ${className}`}>{links.map(([,href,label,Icon])=><Link key={href} href={href} className="inline-flex min-h-11 items-center gap-3 rounded-md border border-border/20 bg-white/5 px-3 text-sm font-semibold text-inherit transition hover:border-primary hover:bg-primary/10 hover:text-primary"><Icon size={18}/>{label}</Link>)}</nav>}
